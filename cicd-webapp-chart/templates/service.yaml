apiVersion: v1
kind: Service
metadata:
  name: {{ include "cicd-webapp-chart.fullname" . }}
  labels:
    {{- include "cicd-webapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cicd-webapp-chart.selectorLabels" . | nindent 4 }}
